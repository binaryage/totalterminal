{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {chooseFile = id; setHotKey = id; showPrefs = id; toggleVisor = id; }; 
            CLASS = VisorController; 
            LANGUAGE = ObjC; 
            OUTLETS = {prefsWindow = NSWindow; statusMenu = NSMenu; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}